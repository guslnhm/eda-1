#include <stdio.h>

int main(){
    int d;
    scanf("%d",&d);
    int a, s, m;
    int codigo, matriculados, aprovados;
    int d[1];
    while(scanf("%d%d%d", &a, &s, &m)!=EOF){
        // EOF no windows: ctrl+z + ENTER
        for(int i=0; i<m; i++){
            scanf("%d%d%d",&codigo, &matriculados, &aprovados);
        }
    }
}